<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_weight="1"
    android:layout_marginEnd="4dp">

    <com.google.android.material.card.MaterialCardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardElevation="0dp"
        app:cardCornerRadius="8dp"
        app:cardBackgroundColor="@android:color/transparent">

        <ImageView
            android:id="@+id/thumbnailImage"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:adjustViewBounds="true"
            android:scaleType="centerCrop"
            android:contentDescription="Ausgewähltes Bild" />

    </com.google.android.material.card.MaterialCardView>

    <!-- Alt-Text Indikator (zeigt an wenn Alt-Text vorhanden) -->
    <ImageView
        android:id="@+id/altTextIndicator"
        android:layout_width="16dp"
        android:layout_height="16dp"
        android:layout_gravity="bottom|start"
        android:layout_margin="6dp"
        android:src="@android:drawable/ic_menu_edit"
        android:background="@drawable/circle_background_dark"
        android:padding="2dp"
        android:visibility="gone"
        app:tint="@color/text_primary"
        android:contentDescription="Alt-Text vorhanden" />

</FrameLayout>
